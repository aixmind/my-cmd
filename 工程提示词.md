想要通过 修改 my-cmd-final.json 中的数据 来达成 一键生成 terminal-commands.html 的目标，我该如何来写 这个工程的提示词呢？包含用什么语言、什么技术等
这种提示词 只适合 从0到1，不适合 已经 调优过的工程 逆向逆推提示词

# 工程提示词：从 JSON 数据生成终端命令参考网页

## 项目概述
创建一个工程，通过读取 `my-cmd-final.json` 中的数据，自动生成 `terminal-commands.html` 文件，实现一键更新终端命令参考网页的目标。

## 技术选型
- **编程语言**：Python 3.8+
- **核心库**：
  - `json`（内置）- 解析 JSON 数据
  - `argparse`（内置）- 命令行参数解析
  - `pathlib`（内置）- 文件路径处理
- **输出格式**：HTML5
- **样式**：内嵌 CSS
- **交互**：内嵌 JavaScript

## 工程结构
```
cmd-reference-generator/
├── generate_html.py        # 主生成脚本
├── my-cmd-final.json       # 命令数据文件
├── terminal-commands.html  # 生成的目标文件
└── README.md               # 使用说明
```

## 核心功能
1. **JSON 解析**：读取并解析 `my-cmd-final.json` 中的命令数据
2. **HTML 生成**：根据解析的数据生成完整的 HTML 结构
3. **样式保持**：确保生成的 HTML 包含与现有文件相同的 CSS 样式
4. **交互功能**：保留命令展开/折叠和复制功能
5. **一键更新**：通过简单命令即可更新整个网页

## 生成逻辑
1. **读取数据**：加载 `my-cmd-final.json` 文件
2. **解析结构**：提取分类、子分类和命令数据
3. **生成 HTML**：
   - 生成头部和导航栏
   - 为每个分类生成对应的标签页
   - 为每个子分类生成可折叠的命令列表
   - 为每个命令生成可展开的选项列表
4. **写入文件**：将生成的 HTML 写入 `terminal-commands.html`

## 命令行接口
```bash
# 基本用法
python generate_html.py

# 指定输入文件
python generate_html.py --input my-cmd-final.json

# 指定输出文件
python generate_html.py --output terminal-commands.html

# 同时指定输入和输出文件
python generate_html.py --input custom-commands.json --output custom-terminal.html
```

## 实现要点
1. **数据结构映射**：
   - JSON 中的 `category` 映射到 HTML 标签页
   - `subcategory` 映射到可折叠的命令列表
   - `commands` 映射到具体命令项
   - `options` 映射到命令选项

2. **HTML 模板**：
   - 保持现有的页面结构和样式
   - 确保响应式设计
   - 保留所有交互功能

3. **CSS 样式**：
   - 内嵌所有必要的 CSS
   - 确保命令代码块左对齐
   - 保留返回顶部按钮样式

4. **JavaScript 功能**：
   - 保留标签页切换功能
   - 保留命令展开/折叠功能
   - 保留命令复制功能
   - 保留返回顶部功能

## 示例代码结构
```python
#!/usr/bin/env python3
import json
import argparse
from pathlib import Path

def load_json(input_file):
    """加载 JSON 数据"""
    with open(input_file, 'r', encoding='utf-8') as f:
        return json.load(f)

def generate_html(data):
    """根据数据生成 HTML"""
    # 生成 HTML 头部
    html = """<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>终端命令参考</title>
    <style>
        /* 完整的 CSS 样式 */
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>终端命令参考</h1>
            <div class="tabs">
                <!-- 标签页将在这里生成 -->
            </div>
        </header>
        <main>
            <!-- 命令内容将在这里生成 -->
        </main>
        <footer>
            <p>© 2024 终端命令参考</p>
            <div class="back-to-top" onclick="scrollToTop()">↑</div>
        </footer>
    </div>
    <script>
        // 完整的 JavaScript 代码
    </script>
</body>
</html>"""
    
    # 替换标签页和命令内容
    # ...
    
    return html

def main():
    """主函数"""
    parser = argparse.ArgumentParser(description='生成终端命令参考网页')
    parser.add_argument('--input', default='my-cmd-final.json', help='输入 JSON 文件路径')
    parser.add_argument('--output', default='terminal-commands.html', help='输出 HTML 文件路径')
    args = parser.parse_args()
    
    input_path = Path(args.input)
    output_path = Path(args.output)
    
    if not input_path.exists():
        print(f'错误：输入文件 {input_path} 不存在')
        return
    
    data = load_json(input_path)
    html = generate_html(data)
    
    with open(output_path, 'w', encoding='utf-8') as f:
        f.write(html)
    
    print(f'成功生成 {output_path}')

if __name__ == '__main__':
    main()
```

## 注意事项
1. **数据格式**：确保 `my-cmd-final.json` 文件格式正确，包含必要的字段
2. **样式一致性**：保持与现有 `terminal-commands.html` 相同的样式和结构
3. **功能完整性**：确保所有交互功能正常工作
4. **错误处理**：添加适当的错误处理，确保生成过程不会因数据问题而失败
5. **编码问题**：确保处理中文字符时使用正确的编码

## 扩展功能（可选）
1. **数据验证**：验证 JSON 数据的完整性和格式
2. **版本控制**：添加版本信息到生成的 HTML
3. **自定义样式**：允许通过配置文件自定义样式
4. **多语言支持**：支持生成不同语言的命令参考
5. **搜索功能**：添加命令搜索功能

通过这个工程，你可以轻松地通过修改 `my-cmd-final.json` 文件来更新终端命令参考网页，实现一键生成的目标。